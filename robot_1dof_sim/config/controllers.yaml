controller_manager:
  ros__parameters:
    update_rate: 100

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    torque_controller:
      type: forward_command_controller/ForwardCommandController
      # (O type: effort_controllers/JointGroupEffortController dependiendo de tu versión)

torque_controller:
  ros__parameters:
    joints:
      - joint1
      - joint2
    interface_name: effort
